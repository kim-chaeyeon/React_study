# 📘 JavaScript 입문 (25.09.26)
## 1. 변수(let): 
let age = 24;  
특징: 프로그램이 실행 되면서 값을 바꿔가면서 저장하는 저장소

## 2. 상수(const):
const name = "KIM";  
특징: 저장한다는 것은 변수와 동일 but **초기화 이후 값 변경이 불가능**

## 3. 변수 명명규칙(Naming Rules)
1. $, _ 제외한 기호는 사용할 수 없다.  
2. 숫자로 시작할 수 없다.  
3. 예약어는 사용할 수 없다.  
